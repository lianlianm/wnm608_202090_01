{
    "notes":[
    	"JSON files always start with either and array [] or and object {}",
    	"JSON files cannot have comments, so put any comments in this array",
    	"Commas go between items in an array or object, but there should lf never be a trailing comma",
    	"All property name must be in quatation marks",
    	"All string quotation marks must be double quotes",
    	"Use a linter like json.com to check json ",
    ]	
}